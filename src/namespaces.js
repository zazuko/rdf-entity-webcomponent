import {
  schema, rdf, rdfs, skos, foaf, dash, xsd, sh, skosxl, vcard
} from '@tpluscode/rdf-ns-builders'

const ns = {
  schema, rdf, rdfs, skos, foaf, dash, xsd, sh, skosxl, vcard
}

export { ns }
